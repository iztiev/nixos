# Unencrypted example — do NOT commit this file.
# Create the real encrypted file with:
#   cd /etc/nixos
#   sops secrets/secrets.yaml
#
# Generate a SHA-512 password hash with:
#   nix-shell -p mkpasswd --run 'mkpasswd -m sha-512'
#
# Then set the contents to:
iztiev/password: '$6$rounds=656000$...'
